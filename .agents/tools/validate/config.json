{
  "logDir": "/tmp/claude-comms-validate",
  "pollIntervalMs": 250,
  "metaGraceMs": 2000,
  "staleLockMs": 1800000,
  "timeoutMs": 2400000,
  "gates": [
    {
      "name": "lint",
      "command": "npm --prefix packages/setup-installer run lint"
    },
    {
      "name": "ts:check",
      "command": "cd workflow-engine && npx tsc --noEmit -p convex/tsconfig.json"
    },
    {
      "name": "test",
      "command": "npx tsx --test .agents/tools/validate/*.test.ts"
    },
    {
      "name": "build",
      "command": "cd workflow-engine/ui && node -e \"const {spawnSync}=require('child_process'); const fs=require('fs'); const path=require('path'); const files=[]; const walk=(dir)=>{ for (const entry of fs.readdirSync(dir,{withFileTypes:true})) { const file=path.join(dir,entry.name); if (entry.isDirectory()) walk(file); else if (entry.isFile() && file.endsWith('.js')) files.push(file); } }; walk('js'); files.sort(); files.push('sw.js'); for (const file of files) { const result=spawnSync(process.execPath,['--check',file],{stdio:'inherit'}); if (result.error) throw result.error; if (result.status !== 0) process.exit(result.status === null ? 1 : result.status); } const required=['index.html','styles.css','js/main.js','js/api.js','manifest.json','sw.js','vercel.json']; for (const file of required) { if (!fs.existsSync(file)) throw new Error('missing '+file); } JSON.parse(fs.readFileSync('vercel.json','utf8')); JSON.parse(fs.readFileSync('manifest.json','utf8')); const html=fs.readFileSync('index.html','utf8'); if (!html.includes('type=\\\"module\\\" src=\\\"./js/main.js\\\"')) throw new Error('index.html missing module entry');\""
    }
  ]
}
