@import './excalidraw.css';

/**
 * Root container inserted by FeedbackOverlayManager.
 * Keep pointer-events disabled on root so it doesn't block page interactions.
 * Child elements explicitly enable pointer-events: auto.
 */
#feedback-overlay-root {
  position: fixed;
  inset: 0;
  z-index: 2147483600;
  pointer-events: none;
  font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
}

/**
 * Base overlay layout
 */
.feedback-widget-overlay {
  position: fixed;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: auto;
  color: #0f172a;
}

.feedback-widget-overlay__backdrop {
  position: absolute;
  inset: 0;
  background: rgba(15, 23, 42, 0.58);
  backdrop-filter: blur(4px);
}

.feedback-widget-overlay__surface {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 16px;
  width: min(960px, 100% - 48px);
  max-height: calc(100vh - 48px);
  padding: 24px;
  border-radius: 16px;
  background: #ffffff;
  box-shadow: 0 20px 45px rgba(15, 23, 42, 0.2);
  overflow: hidden;
}

.feedback-widget-overlay__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.feedback-widget-overlay__title {
  margin: 0;
  font-size: 20px;
  font-weight: 600;
  color: #0f172a;
}

.feedback-widget-overlay__body {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 16px;
  flex: 1;
  min-height: 0;
}

.feedback-widget-overlay__note {
  width: 100%;
  min-height: 120px;
  resize: vertical;
  padding: 12px 14px;
  font: inherit;
  color: inherit;
  border: 1px solid rgba(15, 23, 42, 0.12);
  border-radius: 12px;
  background: rgba(248, 250, 252, 0.72);
}

.feedback-widget-overlay__note:focus {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.15);
  background: #ffffff;
}

.feedback-widget-overlay__actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.feedback-widget-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 10px 18px;
  border: none;
  border-radius: 9999px;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.feedback-widget-button--primary {
  background: linear-gradient(135deg, #2563eb, #3b82f6);
  color: #ffffff;
  box-shadow: 0 12px 24px rgba(37, 99, 235, 0.25);
}

.feedback-widget-button--primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 16px 32px rgba(37, 99, 235, 0.22);
}

.feedback-widget-button--ghost {
  background: rgba(15, 23, 42, 0.08);
  color: #0f172a;
}

.feedback-widget-button--ghost:hover {
  background: rgba(15, 23, 42, 0.14);
}

.feedback-widget-toast {
  position: fixed;
  bottom: 32px;
  right: 32px;
  display: inline-flex;
  align-items: center;
  gap: 12px;
  padding: 12px 20px;
  border-radius: 12px;
  color: #ffffff;
  background: rgba(16, 185, 129, 0.98);
  box-shadow: 0 18px 36px rgba(15, 23, 42, 0.26);
  font-size: 14px;
  font-weight: 500;
  animation: feedback-widget-toast-enter 0.32s ease-out;
  z-index: 2147483640;
}

.feedback-widget-toast--error {
  background: rgba(239, 68, 68, 0.98);
}

@keyframes feedback-widget-toast-enter {
  0% {
    opacity: 0;
    transform: translateY(12px);
  }

  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.feedback-widget-loading-spinner {
  width: 18px;
  height: 18px;
  border-radius: 9999px;
  border: 2px solid rgba(255, 255, 255, 0.35);
  border-top-color: rgba(255, 255, 255, 1);
  animation: feedback-widget-spin 0.8s linear infinite;
}

@keyframes feedback-widget-spin {
  to {
    transform: rotate(360deg);
  }
}

/**
 * Applied by PII redaction utilities
 */
.feedback-widget-redact {
  filter: blur(12px) !important;
  transition: filter 0.12s ease-out;
}

/**
 * Hide helper utility (for temporarily hiding toolbar elements during capture)
 */
[data-feedback-widget-hidden='true'] {
  visibility: hidden !important;
}
